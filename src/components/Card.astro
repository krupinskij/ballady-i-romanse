---
import AnnotationMarker from './AnnotationMarker.astro';

interface Props {
  title: string;
  href: string;
  compact?: boolean;
}

const { title, href, compact } = Astro.props;
const device = Astro.locals.DEVICE;
---

<a class:list={[{ compact }]} href={href} data-device={device}>
  <div><AnnotationMarker body={title} noAnchor /></div>
</a>

<style lang="scss">
  a {
    border: 3px double var(--color-border);
    background-color: var(--color-bg-primary);
    width: 160px;
    aspect-ratio: 2/1;

    display: flex;
    justify-content: center;
    align-items: center;

    &:hover {
      text-decoration: underline;
    }

    & > div {
      font: var(--font-lg);
      text-align: center;
    }
  }

  a.compact {
    aspect-ratio: auto;
    padding: var(--spacing-3) var(--spacing-2);
  }

  a[data-device='desktop'] {
    width: 256px;
  }
</style>
