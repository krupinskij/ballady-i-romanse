---
import type { Annotation } from '@model';

type Props = Annotation;

const { id, body } = Astro.props;
---

<div>
  <a id={`cite_note-${id}`} class="@anno_note" href={`#cite_ref-${id}`}>: </a>
  {body}
</div>

<style>
  div {
    font: var(--font-xs);
    text-wrap: wrap;
  }
</style>
